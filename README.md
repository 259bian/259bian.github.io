HTML 骨架
<meta name="viewport"> 是响应式布局的基础，没有这行，手机浏览器会自动缩放页面导致样式错乱。

导航栏里的 button.nav-toggle 是移动端的汉堡按钮，默认在桌面端隐藏，JavaScript  中会监听点击事件来展开菜单。

section 里的 id 属性对应导航链接的 href ，点击导航可以自动滚动到对应区域。

项目卡片中每个链接先用 # 占位，等部署后替换成真实链接。


